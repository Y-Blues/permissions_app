title: New permission
fields:
  - name: role
    label: Role (identifier)
    required: true
  - name: rights
    label: Rights (comma separated, e.g. read:book, *:*)
    type: list
    required: true
actions:
  - name: submit
    label: Create
    endpoint:
      service: rolePermission
      method: POST
