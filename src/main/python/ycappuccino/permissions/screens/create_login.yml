title: New user — credentials
fields:
  - name: login
    label: Login
    required: true
  - name: password
    label: Initial password
    type: password
    required: true
actions:
  - name: submit
    label: Create the credentials
    endpoint:
      service: create_login
      method: POST
