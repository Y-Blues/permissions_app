title: New organization
fields:
  - name: name
    label: Name
    required: true
  - name: father
    label: Parent organization (optional)
actions:
  - name: submit
    label: Create
    endpoint:
      service: organization
      method: POST
