title: New user — profile
fields:
  - name: name
    label: Display name
    required: true
  - name: login
    label: Login (created in the previous step)
    required: true
  - name: role
    label: Role (identifier)
    required: true
actions:
  - name: submit
    label: Create the account
    endpoint:
      service: account
      method: POST
